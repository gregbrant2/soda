package routing

// func BindRoute(
// 	group *echo.Group,
// 	path string,
// 	handler http.HandlerFunc) {
// 	slog.Debug("Binding route", "path", path, "handler", handler)
// 	group.HandleFunc(path, middlewares.Chain(handler, middlewares.Logging()))
// }
